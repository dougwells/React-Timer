var React = require('react');

var Timer = React.createClass({
  render: function(){
    return (
      <div>
        <h2>Timer Rendered ...</h2>
      </div>
    );
  }
});

module.exports = Timer;

// var Timer = (props) =>
//       <div>
//         <h2>Timer Rendered ...</h2>
//       </div>
//
// module.exports = Timer;
